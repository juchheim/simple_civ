export const HEX_SIZE = 75;
export const MIN_ZOOM = 0.5;
export const MAX_ZOOM = 3.0;
export const ZOOM_WHEEL_SENSITIVITY = 0.0015; // zoom factor per wheel delta unit
export const ZOOM_SMOOTHING = 0.22; // fraction of remaining zoom delta applied each frame (60fps)
export const PAN_INERTIA_DECAY = 0.92; // per-frame decay (60fps) for inertia velocity
export const PAN_INERTIA_MIN_VELOCITY = 0.02; // px/ms threshold to stop inertia
export const UNIT_IMAGE_SIZE = 110; // Larger unit icons
export const DRAG_THRESHOLD = 3; // Pixels before pan
export const RIVER_OPACITY = 0.9;
export const CITY_IMAGE_SIZE = HEX_SIZE * 2;
export const CITY_LABEL_OFFSET = HEX_SIZE * 0.4;

